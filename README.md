# 🍕 PizzaHub - Онлайн-пиццерия

Современная онлайн-пиццерия с полным функционалом управления заказами, пользователями и доставкой.

## 🚀 Возможности

### Для клиентов:
- 📋 **Каталог пицц** с фильтрацией по категориям
- 👤 **Система регистрации и авторизации**
- 🛒 **Корзина** с управлением товарами
- 💳 **Оформление заказа** с доставкой
- 📱 **Адаптивный дизайн** для всех устройств
- 🎨 **Современный UI** с анимациями

### Для администратора:
- 📊 **Панель управления** заказами
- 📈 **Статистика** продаж и пользователей
- 🔄 **Управление статусами** заказов
- 👥 **Управление пользователями**

## 🛠️ Технологии

### Frontend:
- HTML5, CSS3, JavaScript (ES6+)
- Font Awesome для иконок
- CSS Grid и Flexbox для адаптивности
- LocalStorage для сохранения данных

### Backend:
- Node.js + Express.js
- SQLite база данных
- JWT аутентификация
- bcryptjs для хеширования паролей
- CORS для междоменных запросов

## 📦 Установка и запуск

### 1. Клонирование и установка зависимостей:
```bash
git clone <repository-url>
cd pizzahub
npm install
```

### 2. Запуск сервера:
```bash
# Для разработки
npm run dev

# Для продакшена
npm start
```

### 3. Доступ к приложению:
- **Клиентский сайт**: http://localhost:3000
- **Админ-панель**: http://localhost:3000/admin.html

## 🔐 Данные для входа

### Администратор:
- Email: `admin@pizzahub.ru`
- Пароль: `admin123`

### Тестовый пользователь (можно создать через регистрацию):
- Любой email и пароль при регистрации

## 📁 Структура проекта

```
pizzahub/
├── index.html          # Главная страница
├── admin.html          # Админ-панель
├── style.css           # Стили
├── script.js           # Клиентский JavaScript
├── server.js           # Backend сервер
├── package.json        # Зависимости проекта
├── README.md           # Документация
└── pizzahub.db         # База данных SQLite (создается автоматически)
```

## 🗄️ База данных

### Таблицы:
- **users** - Пользователи
- **pizzas** - Пиццы
- **orders** - Заказы
- **order_items** - Элементы заказов

### API эндпоинты:

#### Аутентификация:
- `POST /api/register` - Регистрация
- `POST /api/login` - Вход
- `GET /api/user` - Информация о пользователе
- `PUT /api/user` - Обновление данных пользователя

#### Пиццы:
- `GET /api/pizzas` - Получение списка пицц
- `GET /api/pizzas?category=meat` - Фильтрация по категории

#### Заказы:
- `POST /api/orders` - Создание заказа
- `GET /api/orders` - Получение заказов пользователя

#### Админ-панель:
- `GET /api/stats` - Статистика

## 🎨 Особенности дизайна

- **Современный градиентный дизайн**
- **Плавные анимации** и переходы
- **Адаптивная верстка** для всех устройств
- **Интерактивные элементы** с hover-эффектами
- **Модальные окна** для форм и корзины
- **Уведомления** о действиях пользователя

## 🚀 Развертывание

### Для продакшена:
1. Установите переменные окружения:
   ```bash
   export NODE_ENV=production
   export PORT=3000
   export JWT_SECRET=your-secret-key
   ```

2. Запустите с помощью PM2:
   ```bash
   npm install -g pm2
   pm2 start server.js --name "pizzahub"
   ```

### Для Docker:
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 3000
CMD ["npm", "start"]
```

## 🔧 Настройка

### Изменение порта:
В файле `server.js` измените константу `PORT` или используйте переменную окружения.

### Добавление новых пицц:
Через базу данных или через админ-панель (в будущем).

### Настройка email-уведомлений:
В файле `server.js` добавьте конфигурацию nodemailer.

## 🐛 Возможные проблемы

1. **Порт занят**: Измените порт в `server.js`
2. **База данных не создается**: Проверьте права на запись в папке
3. **CORS ошибки**: Убедитесь что backend запущен и доступен

## 📈 Планы по развитию

- [ ] Интеграция с платежными системами
- [ ] Email и SMS уведомления
- [ ] Отзывы и рейтинги пицц
- [ ] Программа лояльности
- [ ] Мобильное приложение
- [ ] Интеграция с картами для отслеживания доставки

## 📄 Лицензия

MIT License

## 👥 Команда

Разработано с ❤️ для демонстрации современных веб-технологий

---

**Наслаждайтесь вкусной пиццей!** 🍕
